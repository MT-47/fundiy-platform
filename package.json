{
  "name": "supportify-platform",
  "version": "1.0.0",
  "description": "Crowdfunding Platform - ITI JS Project",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT --static public"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}